# Copy to .env and adjust as needed.
# Dev: "/api" is proxied to http://localhost:5056 (see vite.config.ts).
# Prod: set to the deployed API origin, e.g. https://api.example.com/api
VITE_API_BASE_URL=/api
